#Requires -Version 5.1
[CmdletBinding()]
param()

Write-Warning 'rebuild.ps1 is deprecated. Use WinTune.ps1 as the canonical entrypoint and edit source files directly with versioned backups instead of auto-rebuilding generated main-entry code.'
exit 0
